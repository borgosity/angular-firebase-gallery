import { OnlyImgClickDirective } from './only-img-click.directive';

describe('OnlyImgClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyImgClickDirective();
    expect(directive).toBeTruthy();
  });
});
