import { OnlyAlbumClickDirective } from './only-album-click.directive';

describe('OnlyAlbumClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlbumClickDirective();
    expect(directive).toBeTruthy();
  });
});
